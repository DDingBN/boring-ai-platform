{
  "name": "@repo/server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run python -m app",
    "build": "uv run python -m compileall -q app",
    "start": "uv run python -m app",
    "test": "uv run pytest"
  }
}
