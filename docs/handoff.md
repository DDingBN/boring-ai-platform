# 开发交接

本页用于在 Windows、macOS 和不同会话之间传递最近工作上下文。架构原则与启动方式见
[README](../README.md)，完整能力和后续任务见 [项目进度](project-status.md)。
历史改动通过 Git 追溯，代码和测试是实现状态的最终依据。

## 当前快照

- 更新时间：2026-09-16。
- 分支：`main`。
- 本次基线：`ac1e061 docs：更新`；开始时工作区干净，与本地远程跟踪分支 `origin/main`
  无领先或落后提示。本次未 fetch、提交或 push。
- 当前目标：D04 类和对象学习已完成，下一次从 D05 等待与清理开始。
- 当前阶段：Vue + FastAPI 最小聊天闭环已经跑通，下一阶段尚未开始。
- 学习下一步：D05 先解释 `async`、`await` 与普通调用的区别，再用独立小例子学习等待和 `finally`。
- 功能开发优先级仍见项目进度：SSE 流式聊天与取消，暂不同时展开数据库和 Agent。

## 最近完成

### 2026-09-16：D04 收尾完成

- 最终核对 `learning/day04.py`：学习者已将 `__init__` 返回注解修正为 `-> None`，
  `prefix`、`content` 与 `reply` 的字符串注解正确。
- 已有正常三行输出、漏传 `prefix` 的 `TypeError` 及恢复运行证据；本轮仅检查注解修正，
  未重复运行脚本或应用 lint、测试、构建。
- 已对照 Mock 类，并解释 `Protocol` 声明与具体实现的区别：声明 `-> str` 不会自动保证
  运行结果；继承而未覆盖该协议方法时，执行其仅有文档字符串的方法体会得到 `None`。
- 已解释实例方法第一个参数接收实例、`self` 是惯用名称且由调用自动传入；实例方法可以
  只有 `self`，不要求其他参数。学习者表示本课没有疑问，D04 收尾，下一课 D05。
- 未修改学习者代码；保留已有工作区修改，仅更新交接记录。

### 2026-09-16：D04 失败实验与类型注解审查

- 学习者提交 `echo1 = Echo()` 的错误记录：第 10 行触发 `TypeError`，缺少 `prefix`。
  已解释异常未捕获时后续打印不会执行。
- 本地确认已恢复 `Echo("助手：")`，并为参数和返回值添加注解；`reply` 注解正确，
  `__init__` 当前误写 `-> str`，下一步提示改为 `-> None`，不代改作业。
- 恢复后用项目解释器（`-X utf8 -B`）运行成功，三行输出保持正确，退出码 0。
  这也说明错误的类型注解本身不会自动触发运行时校验。
- 本轮仅审查练习并更新交接；未运行应用 lint、全量测试或构建，D04 尚待注解修正与 Mock 对照。

### 2026-09-16：D04 双实例正常验证通过

- 已读取学习者编写的 `learning/day04.py`：`Echo` 初始化保存 `self.prefix`，
  `reply` 拼接实例前缀和本次文字，两个实例分别使用“客服：”和“助手：”。
- 学习者正确解释两个实例各自保存前缀，创建第二个不会影响第一个。
- 在 Server 目录用项目解释器运行（`-X utf8 -B`），实际依次输出“客服：你好”、
  “助手：你好”、“客服：再见”，退出码 0。沙箱内启动被拒绝，经工具审批后在沙箱外运行成功。
- 未修改练习代码；下一步提示学习者临时漏传 `prefix`，提交错误记录，再讲类型注解与 Mock 类。
- 本轮仅更新交接记录，未运行应用 lint、全量测试或构建。

### 2026-09-16：D04 开课，等待学习者尝试

- 已读取原学习大纲 D04 和现有 `app/llm/mock.py`，准备以独立 `Echo` 类解释
  `class`、实例、`__init__`、`self`、属性与方法调用。
- 本轮提供单实例起步示例和双实例练习提示，不代写学习者作业，不标记 D04 完成。
- 后续依次审查正常输出、验证漏传初始化参数、补充 `str` 注解并对照 Mock 类。
- D03 运行证据已通过；此前抛出与捕获的复述尚未记录确认，开课时简短回顾。
- 保留已有工作区修改；仅更新交接页，未运行应用 lint、测试或构建。

### 2026-09-16：D03 单元 1 完成，单元 2 待尝试

- 已核对外部学习大纲与 D02 脚本，沿用 `make_reply` 的 `woc` 前缀。
- 已读取学习者创建的 `text_helpers.py` 和 `day03.py`：正确导入函数，循环内调用并打印。
- 学习者提供输出 `woc你好`、`woc我在学习Python`，并正确解释两轮循环变量的值。
- 已审查空白判断：函数以 `content.strip()` 的结果判断空字符串，并抛出 `ValueError`。
- 学习者提供正常第一条输出和第二条空格输入的完整错误堆栈，异常从 `text_helpers.py` 第 4 行
  传回 `day03.py` 第 6 行；下一步在循环内使用 `try/except ValueError` 捕获并验证继续处理。
- 已审查循环内的 `try/except ValueError`，运行确认正常回复、空白提示及继续处理第三条均符合预期。
- 学习者将抛出与捕获的表述说反，正在结合 `raise` 与 `except` 纠正；待复述确认后再结束 D03。
- 保留进入会话时已有的交接页修改和 `apps/server/learning/` 文件；本轮仅更新交接记录，
  未运行应用 lint、测试或构建。

### 2026-09-16：D02 第一段 Python

- 学习者新增 `apps/server/learning/day02.py`，能够说明 `.py` 文件由解释器执行，字符串从
  字典进入函数参数，函数返回值进入列表后由 `print` 输出。
- 学习者把输入改为 `nb`、回复前缀改为 `woc`，运行得到 `wocnb`。
- 将取值键由 `content` 临时改为不存在的 `contents`，观察到 `KeyError: 'contents'`，
  能根据错误末行和对应代码位置判断字典键不一致；恢复后再次得到 `wocnb`。
- 本单元只新增独立学习脚本，没有修改或注册项目业务 API。

### 2026-09-16：D01 单请求观察

- 学习者通过浏览器 Network 观察了聊天页面的一次成功请求，确认 `POST`
  `/api/v1/chat/messages`、JSON 请求体、HTTP `200` 与页面助手消息之间的对应关系。
- 直接请求不存在的 Server 地址，确认 `GET` 请求得到 HTTP `404`、统一错误消息和
  `data.requestId`；能够说明浏览器发送、服务端响应和前端渲染的基本过程。
- 本单元只做运行与观察，没有修改业务代码、API 契约或测试。

### 2026-09-16：学习计划修订

- 已写回用户的原文档：`C:\Users\DDingBN\Desktop\boring-notes\+\Boring AI Platform.md`。
  该文件在仓库外，本次没有新增仓库内的第二份学习计划；跨设备需单独同步笔记。
- 原 24 课改为 M01～M24 能力模块，补充 D01～D12 入门学习日、SQL/事务与异步迭代等先修。
- 按每天 4～6 小时估算 40～53 个学习日，阶段 A 后按实际表现校准。
- 第一份作业聚焦一次请求与响应；两轮对话实验移到 M11，验收新实现的上下文能力。
- 增加练习位置、运行目录、提示与审查流程及正常/失败证据；保留原有工程验收。
- 学习启动用两个终端直接运行 Web/Server，并明确使用 Mock；未修改项目启动脚本。
- 只修订学习文档和本交接页，没有创建练习代码、修改业务实现或标记课程功能完成。

### 2026-09-15：文档整理

- `README.md` 收录迁移背景和四条架构原则，统一安装说明与文档导航。
- `docs/project-status.md` 合并 P0/P1/P2 后续任务、非目标及待补设计，保留原有任务细节。
- `docs/api.md` 明确 `501` 规划接口的参数与成功响应尚未实现；`docs/database.md`
  明确全文为待实现的数据设计。
- 移除已合并的独立迁移记录，同步 `AGENTS.md` 阅读顺序和文档维护约定。
- 本页精简为近期改动、验证和下一步；未改变业务实现或架构决策。

## 验证

### 本次验证（2026-09-16）

- D03 单元 1：本地代码审查与学习者两条正常回复的运行记录相符。
- D03 单元 2：本地列表当前为 `['你好', ' ']`，代码与学习者提供的 `woc你好` 和
  `ValueError: 输入不能为空` 堆栈相符。
- D03 捕获验证：当前列表为 `['你好', ' ', '我在学习Python']`，项目解释器运行成功，依次输出
  `woc你好`、`处理失败：输入不能为空`、`woc我在学习Python`，退出码 0。
  沙箱内解释器启动被拒绝，经工具审批在沙箱外运行；使用 `-X utf8 -B` 核对中文输出且不生成字节码。
- D02 正常情况：学习者使用项目解释器运行 `learning/day02.py`，实际输出为 `wocnb`；
  仓库文件已核对恢复为访问 `message["content"]`。
- D02 失败情况：访问不存在的 `message["contents"]` 时得到 `KeyError: 'contents'`，错误定位到
  `day02.py` 第 5 行；恢复正确键后再次运行成功。
- D01 成功情况：Mock Chat 返回 HTTP `200`，`data.message.content` 为
  `Python 服务端已收到：你好`，页面显示相同文字。
- D01 失败情况：`GET /api/v1/learning-not-found` 返回 HTTP `404`，响应为项目统一错误结构，
  包含“请求的接口不存在。”和以 `req_` 开头的请求 ID。
- 学习计划结构检查通过：12 个入门学习日、24 个模块及旧课号对应完整，阶段预算合计 40～53 天。
- 核对 D01 和第一份作业为单请求观察，两轮上下文实验只安排在 M11。
- 3 个 Python 起步代码块运行通过；最小 FastAPI 示例的成功与 `404` 响应验证通过。
- 项目 Python 在沙箱内启动曾被拒绝；经工具审批后在沙箱外完成上述本地示例验证，未调用真实模型。
- 学习计划和本页 Prettier 检查、仓库 `git diff --check` 通过；3 个本地文档链接有效。
- 写回前核对原文未变，写回后核对与审查稿一致；临时目录保留原稿备份。
- 本次没有业务代码改动，未重跑应用 lint、全量测试或构建。

### 上次文档检查（2026-09-15，历史记录）

- 本地 Prettier 格式检查通过，覆盖 README、AGENTS 与四份 docs 文档。
- 文档结构检查通过：18 个仓库内链接有效，12 段 JSON 示例可解析，代码围栏配对完整。
- 旧文档引用检查和 `git diff --check` 通过；复核确认原有四项架构决策、任务细节及非目标已保留。
- `pnpm exec prettier` 在当前环境未能解析命令；改用
  `node node_modules/prettier/bin/prettier.cjs` 完成格式化与检查，未安装额外依赖。
- 本次只改文档，未重跑应用 lint、测试或构建。

### 上一次应用验证（2026-09-14）

以下是历史验收结果，不代表本次重新运行：

- Server：pytest `4 passed`；Web：ESLint 和 Vite 生产构建通过。
- 根目录：`pnpm lint`、`pnpm test`、`pnpm build` 通过。
- 联调：通过 Vite `/api` 代理完成两次 Chat 请求，确认 `conversationId` 保持不变；
  这不代表服务端保存或传递了历史上下文。
- DeepSeek：仅使用测试密钥完成本地初始化验证，未发起外部模型请求。

## 阻塞与已知事项

- 当前无阻塞。后续数据库阶段仍需补齐 RunStep、工具消息和审批状态的设计。
- 此前测试有 AnyIO deprecation warning；Vite 提示主 JavaScript chunk 超过 500 kB。
- Python `compileall` 不生成 `dist`，Turbo 会提示 Server build 没有匹配的输出文件。
- 全仓库格式规范化仍是后续任务，本次仅检查整理涉及的文档。

## 下一步与接续

接续 D05：D04 正常、漏参失败、恢复运行、注解修正与 Mock 类对照已完成。
下一课先解释普通函数调用与等待异步结果，再示范 `asyncio.sleep` 和 `asyncio.run`，
按原大纲学习成功和失败时的 `finally` 清理；继续先提示、学习者尝试、审查后验证的节奏。
保留 D03 回顾：`raise` 抛出异常，`except` 捕获异常；不将尚未收到的复述记为已确认。
项目功能任务优先级仍统一维护在 [项目进度](project-status.md)，不以学习计划修订代替功能完成。

新设备先按 [协作说明](../AGENTS.md) 检查 Git 状态与阅读文档，再参考
[README](../README.md) 安装和启动。每次结束时更新本页的当前目标、已完成、验证、阻塞与下一步，
并按实际情况提交和推送。
