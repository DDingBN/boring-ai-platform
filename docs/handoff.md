# 开发交接

本页用于在 Windows、macOS 和不同会话之间传递最近工作上下文。架构原则与启动方式见
[README](../README.md)，完整能力和后续任务见 [项目进度](project-status.md)。
历史改动通过 Git 追溯，代码和测试是实现状态的最终依据。

## 当前快照

- 更新时间：2026-09-16。
- 分支：`main`。
- 本次基线：`ac1e061 docs：更新`；开始时工作区干净，与本地远程跟踪分支 `origin/main`
  无领先或落后提示。本次未 fetch、提交或 push。
- 当前目标：按修订后的学习计划带学；D01 已完成，准备进入 D02 Python 基础。
- 当前阶段：Vue + FastAPI 最小聊天闭环已经跑通，下一阶段尚未开始。
- 学习下一步：进入 D02，运行并理解第一段 Python；先完成基础再进入功能开发。
- 功能开发优先级仍见项目进度：SSE 流式聊天与取消，暂不同时展开数据库和 Agent。

## 最近完成

### 2026-09-16：D01 单请求观察

- 学习者通过浏览器 Network 观察了聊天页面的一次成功请求，确认 `POST`
  `/api/v1/chat/messages`、JSON 请求体、HTTP `200` 与页面助手消息之间的对应关系。
- 直接请求不存在的 Server 地址，确认 `GET` 请求得到 HTTP `404`、统一错误消息和
  `data.requestId`；能够说明浏览器发送、服务端响应和前端渲染的基本过程。
- 本单元只做运行与观察，没有修改业务代码、API 契约或测试。

### 2026-09-16：学习计划修订

- 已写回用户的原文档：`C:\Users\DDingBN\Desktop\boring-notes\+\Boring AI Platform.md`。
  该文件在仓库外，本次没有新增仓库内的第二份学习计划；跨设备需单独同步笔记。
- 原 24 课改为 M01～M24 能力模块，补充 D01～D12 入门学习日、SQL/事务与异步迭代等先修。
- 按每天 4～6 小时估算 40～53 个学习日，阶段 A 后按实际表现校准。
- 第一份作业聚焦一次请求与响应；两轮对话实验移到 M11，验收新实现的上下文能力。
- 增加练习位置、运行目录、提示与审查流程及正常/失败证据；保留原有工程验收。
- 学习启动用两个终端直接运行 Web/Server，并明确使用 Mock；未修改项目启动脚本。
- 只修订学习文档和本交接页，没有创建练习代码、修改业务实现或标记课程功能完成。

### 2026-09-15：文档整理

- `README.md` 收录迁移背景和四条架构原则，统一安装说明与文档导航。
- `docs/project-status.md` 合并 P0/P1/P2 后续任务、非目标及待补设计，保留原有任务细节。
- `docs/api.md` 明确 `501` 规划接口的参数与成功响应尚未实现；`docs/database.md`
  明确全文为待实现的数据设计。
- 移除已合并的独立迁移记录，同步 `AGENTS.md` 阅读顺序和文档维护约定。
- 本页精简为近期改动、验证和下一步；未改变业务实现或架构决策。

## 验证

### 本次验证（2026-09-16）

- D01 成功情况：Mock Chat 返回 HTTP `200`，`data.message.content` 为
  `Python 服务端已收到：你好`，页面显示相同文字。
- D01 失败情况：`GET /api/v1/learning-not-found` 返回 HTTP `404`，响应为项目统一错误结构，
  包含“请求的接口不存在。”和以 `req_` 开头的请求 ID。
- 学习计划结构检查通过：12 个入门学习日、24 个模块及旧课号对应完整，阶段预算合计 40～53 天。
- 核对 D01 和第一份作业为单请求观察，两轮上下文实验只安排在 M11。
- 3 个 Python 起步代码块运行通过；最小 FastAPI 示例的成功与 `404` 响应验证通过。
- 项目 Python 在沙箱内启动曾被拒绝；经工具审批后在沙箱外完成上述本地示例验证，未调用真实模型。
- 学习计划和本页 Prettier 检查、仓库 `git diff --check` 通过；3 个本地文档链接有效。
- 写回前核对原文未变，写回后核对与审查稿一致；临时目录保留原稿备份。
- 本次没有业务代码改动，未重跑应用 lint、全量测试或构建。

### 上次文档检查（2026-09-15，历史记录）

- 本地 Prettier 格式检查通过，覆盖 README、AGENTS 与四份 docs 文档。
- 文档结构检查通过：18 个仓库内链接有效，12 段 JSON 示例可解析，代码围栏配对完整。
- 旧文档引用检查和 `git diff --check` 通过；复核确认原有四项架构决策、任务细节及非目标已保留。
- `pnpm exec prettier` 在当前环境未能解析命令；改用
  `node node_modules/prettier/bin/prettier.cjs` 完成格式化与检查，未安装额外依赖。
- 本次只改文档，未重跑应用 lint、测试或构建。

### 上一次应用验证（2026-09-14）

以下是历史验收结果，不代表本次重新运行：

- Server：pytest `4 passed`；Web：ESLint 和 Vite 生产构建通过。
- 根目录：`pnpm lint`、`pnpm test`、`pnpm build` 通过。
- 联调：通过 Vite `/api` 代理完成两次 Chat 请求，确认 `conversationId` 保持不变；
  这不代表服务端保存或传递了历史上下文。
- DeepSeek：仅使用测试密钥完成本地初始化验证，未发起外部模型请求。

## 阻塞与已知事项

- 当前无阻塞。后续数据库阶段仍需补齐 RunStep、工具消息和审批状态的设计。
- 此前测试有 AnyIO deprecation warning；Vite 提示主 JavaScript chunk 超过 500 kB。
- Python `compileall` 不生成 `dist`，Turbo 会提示 Server build 没有匹配的输出文件。
- 全仓库格式规范化仍是后续任务，本次仅检查整理涉及的文档。

## 下一步与接续

按外部学习计划进入 D02：先解释 Python 解释器、`.py` 文件、缩进、字符串、函数参数与返回值，
再运行起步示例并给一个小变化任务；不提前要求编写测试替身。
项目功能任务优先级仍统一维护在 [项目进度](project-status.md)，不以学习计划修订代替功能完成。

新设备先按 [协作说明](../AGENTS.md) 检查 Git 状态与阅读文档，再参考
[README](../README.md) 安装和启动。每次结束时更新本页的当前目标、已完成、验证、阻塞与下一步，
并按实际情况提交和推送。
